import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hn.benoiteirik',
  appName: "L'Homme Nouveau",
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
